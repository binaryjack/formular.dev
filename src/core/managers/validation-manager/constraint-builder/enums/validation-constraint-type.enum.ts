export enum ValidationConstraintTypeEnum {
    required = 'required',
    min = 'min',
    max = 'max',
    minLength = 'minLength',
    maxLength = 'maxLength',
    pattern = 'pattern',
    custom = 'custom'
}
