import * as DrawerBaseInput from './drawer-base-input'
import * as DrawerBaseInputTypes from './drawer-base-input.types'

describe('drawer-base variant exports', () => {
    it('should export objects and functions', () => {
        expect(DrawerBaseInput).toBeDefined()
        expect(DrawerBaseInputTypes).toBeDefined()
    })
})
