import { ICommandManager } from '../command-manager.types'

export const setup = function (this: ICommandManager) {
    // this.accept(
    //     notify(
    //         `${this.editorElement.id}_format_text_${this.name}`,
    //         this.notifyStateChanges.bind(this),
    //         'formattingStateChanged'
    //     )
    // )
}
