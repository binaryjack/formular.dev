import { IRtiEngine } from '../rtiEngine.types'

export const cleanHtml = function (this: IRtiEngine): void {
    // First sanitize to remove any dangerous content
    // this.sanitizeHtml()
    // Then normalize to ensure clean structure
    // this.normalizeHtml()
}
