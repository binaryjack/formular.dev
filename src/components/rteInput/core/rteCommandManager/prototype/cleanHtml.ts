import { IRteCommandManager } from '../rteCommandManager.types'

export const cleanHtml = function (this: IRteCommandManager): void {
    // First sanitize to remove any dangerous content
    // this.sanitizeHtml()
    // Then normalize to ensure clean structure
    // this.normalizeHtml()
}
