import { extend } from './extend'
import { merge } from './merge'
import { omit } from './omit'
import { partial } from './partial'
import { pick } from './pick'
import { required } from './required'

export { extend, merge, omit, partial, pick, required }

export default {
    partial,
    required,
    pick,
    omit,
    extend,
    merge
}
