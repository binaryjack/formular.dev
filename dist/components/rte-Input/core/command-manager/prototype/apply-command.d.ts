import { IRteCommand } from '../../rti-engine.types';
import { ICommandManager } from '../command-manager.types';
export declare const applyCommand: (this: ICommandManager, command: IRteCommand) => void;
