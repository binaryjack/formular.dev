import { ISelection } from '../../rti-engine.types';
import { ISelectionManager } from '../selection-manager.types';
export declare const restoreSelection: (this: ISelectionManager, selection: ISelection) => null | undefined;
