import { ElementPositionOutputType } from '../../../style/global.types';
interface DrawerSlotProps {
    id: string;
    slotName: string;
    opensToThe: ElementPositionOutputType;
}
export declare const DrawerSlotCenter: ({ id, slotName, opensToThe }: DrawerSlotProps) => import("@emotion/react/jsx-runtime").JSX.Element;
export {};
