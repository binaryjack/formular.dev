import { ITab } from '../types/i-tab';
export declare const newTab: (id: string, label: string, icon?: React.ReactNode, disabled?: boolean, selected?: boolean, onClick?: (id: string) => void) => ITab;
