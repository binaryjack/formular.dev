/**
 * Legacy Global Styles - TO BE DEPRECATED
 * 
 * These styles are being migrated to the design system or app-specific files.
 * Please use design system components and utilities instead of these classes.
 * 
 * MIGRATION STATUS:
 * ✅ Text ellipsis utilities → Use .text-ellipsis from design system
 * ✅ Flex layout utilities → Use .flex-between, .flex-center from design system  
 * ✅ Text size utilities → Use .text-micro, .text-mini, etc. from design system
 * ⚠️  Remaining utilities below need manual migration
 */
@layer base {
    body {
        width: 100vw;
        height: 100vh;
        overflow-y: auto;
        overflow-x: hidden;
    }
}

@layer utilities {

    /* DEPRECATED: Use .text-micro from design system instead */
    .text-04 {
        @apply text-[0.4em];
    }

    /* DEPRECATED: Use .text-mini from design system instead */
    .text-08 {
        @apply text-[0.8em];
    }

    /* DEPRECATED: Use .text-base-em from design system instead */
    .text-10 {
        @apply text-[1em];
    }

    /* DEPRECATED: Use .text-large-em from design system instead */
    .text-115 {
        @apply text-[1.15em];
    }

    /* DEPRECATED: Use .text-xl-em from design system instead */
    .text-135 {
        @apply text-[1.35em];
    }

    /* DEPRECATED: Use .text-2xl-em from design system instead */
    .text-165 {
        @apply text-[1.65em];
    }

    /* DEPRECATED: Use .text-3xl-em from design system instead */
    .text-200 {
        @apply text-[2em];
    }

    /* DEPRECATED: Use .text-ellipsis from design system instead */
    .elipsis {
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
    }

    /* DEPRECATED: Use .text-ellipsis from design system instead */
    .text-elipsis {
        @apply whitespace-nowrap overflow-hidden overflow-ellipsis;
    }

    /* DEPRECATED: Use .flex-between from design system instead */
    .flex-between {
        @apply flex justify-between items-center;
    }

    /* TODO: Evaluate if these should be moved to design system */
    .items-left {
        @apply flex items-start flex-col bg-slate-600 p-1 rounded;
    }

    .icon-box {
        @apply bg-gray-200 p-2 rounded w-8 mt-0 transition ease-in-out delay-150 hover:bg-gray-300 text-xs/6;
    }
}