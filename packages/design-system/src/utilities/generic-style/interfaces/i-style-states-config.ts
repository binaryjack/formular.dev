/**
 * Configuration for which style states are enabled for a component
 */
export interface IStyleStatesConfig {
    hasHover?: boolean
    hasRing?: boolean
    hasFocused?: boolean
    hasPressed?: boolean
    hasDisable?: boolean
    hasErrors?: boolean
}
