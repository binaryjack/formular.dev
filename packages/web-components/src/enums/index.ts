// Export all enums
// Following CONTRIBUTING.md guidelines: one enum per file, suffixed with 'Enum'

// Placeholder - enums will be exported here as they are created
// Example:
// export * from './form-validation-state.enum';
// export * from './input-type.enum';

// For now, create a placeholder export to avoid module errors
export const ENUMS_MODULE_PLACEHOLDER = 'enums-module';
